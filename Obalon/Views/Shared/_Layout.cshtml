@using Obalon.Utils;

<!DOCTYPE html>
@{
    string currentRouteController = ViewContext.RouteData.Values["controller"].ToString();
    string currentRouteId = ViewContext.RouteData.Values["id"] == null ? "" : ViewContext.RouteData.Values["id"].ToString();

    //string localeZone = ViewHelper.Instance.LocaleIsNcsa(language) ? "NCSA" : "EMEA";

    //var userIsLoggedIn = ViewHelper.Instance.UserLoggedIn(Request);

    PortalController portalController = ViewHelper.Instance.GetCurrentController(currentRouteController);

    //string currentUplayConnectEnvironment = ConfigurationManager.AppSettings["UplayConnectEnvironment"];
    //string uplayCdnName = ViewHelper.Instance.GetUplayCdnName();

    //bool isLocalhostUrl = Request.Url.AbsoluteUri.Contains("localhost");

    //string disableChromeClassValue = ViewHelper.Instance.GetDisableChromeClassValue(Request, Response);
    //string languageIsoTwoLetters = string.IsNullOrEmpty(language) || !language.Contains("-") ? "en" : language.Split('-')[0];
    //string dir = languageIsoTwoLetters == "ar" ? "rtl" : "ltr";

    //string typekitJsFileName = ViewHelper.GetTypeKitFilename(languageIsoTwoLetters) + ".js";
}
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <div id="showLogin" class="btn btn-entity pull-right" onclick="window.login.popup('showLogin','loginPopup')">
        Login
    </div>

    @Html.Partial("~/Views/Shared/_LoginPopup.cshtml")

    <div id="wrapper">
        <div class="container body-content">
            <div class="row">
                <div class="navbar-default sidebar col-lg-3 col-md-3" role="navigation">
                    <div class="sidebar-nav navbar-collapse">
                        <ul class="nav in" id="side-menu">
                            <li>
                                <img src="~/Content/Images/balloon.jpg" style="max-width:70%" alt="logo" />
                            </li>
                            <li>
                                <a href="#" class="btn btn-primary btn-md"><i class="fa fa-dashboard fa-fw"></i> Dashboard</a>
                            </li>
                            <li>
                                @Html.ActionLink("My Patients", "Index", "Patient", new { id = "" }, null)
                            </li>
                            <li>
                                <a href="#" class=""><i class="fa fa-table fa-fw"></i> Sign out</a>
                            </li>
                        </ul>
                    </div>
                </div>


                <div class="col-lg-9 col-md-9">
                    @RenderBody()
                    <hr />
                </div>
            </div>
        </div>

    </div>

    @*<script src="~/Scripts/Common/auth.js"></script>*@

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/common")
    @Scripts.Render("~/bundles/jqueryval")
    @RenderSection("scripts", required: false)


    <script>

        $(document).mouseup(function (e) {
            var container = $("#loginPopup");
            if (!container.is(e.target) && container.has(e.target).length === 0)
                container.hide();
        });

    </script>

</body>
</html>
